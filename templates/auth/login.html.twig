{% extends "layout.html.twig" %}

{% block content %}
    <form method="post" action="{{ path('login') }}" class="form-horizontal">
        {{ form_widget(form) }}

        <div class="col-sm-10 col-sm-offset-2">
            <button type="submit" class="btn btn-primary">Log in</button>
        </div>
    </form>
{% endblock %}
